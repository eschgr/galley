# Sibling document

You navigated here from a local-file link in `link-handling.md`. If this opened in
a new tab, relative-path link handling works.

## Intro

This is the section a `./sibling.md#intro` link points at. (Opening a file link
with a `#fragment` currently opens the file at the top; jumping to the fragment in
the newly opened tab is a possible future refinement.)
